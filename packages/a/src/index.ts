import { add } from "@choseohwan-temp/b";
import { minus } from "@choseohwan-temp/c";
import { multiply } from "@choseohwan-temp/c/multiply";

const sum = add(1, 2);
const diff = minus(5, 4);
const multi = multiply(6, 9);
console.log(sum, diff, multi);
